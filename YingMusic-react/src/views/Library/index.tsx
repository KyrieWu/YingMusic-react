import React from 'react';

import styles from './style.module.scss';

const Library: React.FC = () => {
	return <main className={styles.library_container}>Library</main>;
};

export default Library;
